import React from 'react'
import { HomeButton } from '../../components';

export default function NotFound() {
    return (
        <div>
            <HomeButton />
            <h1>
                Page not found.
            </h1>
        </div>
    )
}
